This level requires us to inject 3 `<input>` text boxes into the website. We can do this by putting 3 input tags into the input field:

```html
<input type='text'><input type='text'><input type='text'>
```